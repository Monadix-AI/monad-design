interface:
  display_name: "Monad Design Color"
  short_description: "Refine native color in the running app"
  default_prompt: "Use $monad-design-color to refine the selected native UI within the current request."
