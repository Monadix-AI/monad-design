interface:
  display_name: "Monad Design Layout"
  short_description: "Refine native layout in the running app"
  default_prompt: "Use $monad-design-layout to refine the selected native UI within the current request."
